# Findings that are deliberate, listed by fingerprint so they are exempt one at
# a time. Allowlisting the file instead would hide the next real one.

# Credentials for the ClickHouse service container that exists only for the
# duration of a CI job. They authenticate nothing outside that job, and they are
# in the workflow because the service is configured from the same file.
3177840a22379730793f434175490b44decd085b:.github/workflows/ci.yml:curl-auth-user:74

# The same finding as seen by the working-tree scan, which fingerprints by path
# and line rather than by commit.
/repo/.github/workflows/ci.yml:curl-auth-user:74
.github/workflows/ci.yml:curl-auth-user:74
